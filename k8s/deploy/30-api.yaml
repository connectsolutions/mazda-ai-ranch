apiVersion: apps/v1
kind: Deployment
metadata:
  name: ranch-api
  namespace: platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ranch-api
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: ranch-api
    spec:
      serviceAccountName: ranch-api
      imagePullSecrets:
        - name: ghcr
      initContainers:
        - name: prisma-migrate
          image: ghcr.io/cleanslice/ranch-api:latest
          imagePullPolicy: Always
          command: ["sh", "-c"]
          args:
            - |
              set -e
              echo "[migrate] applying prisma migrations"
              bunx prisma migrate deploy --schema=./prisma/schema.prisma
              echo "[migrate] done"
          env:
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ranch-external-db
                  key: uri
      containers:
        - name: api
          image: ghcr.io/cleanslice/ranch-api:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 3333
          env:
            - name: NODE_ENV
              value: "dev"
            - name: PORT
              value: "3333"
            # Trust the k3s API server's self-signed CA for in-cluster API
            # calls (KubePodGateway watches, log streaming via CoreV1Api).
            # The CA is auto-mounted by the ServiceAccount projection.
            - name: NODE_EXTRA_CA_CERTS
              value: /var/run/secrets/kubernetes.io/serviceaccount/ca.crt
            - name: CORS_ORIGIN
              value: "https://ranch.cleanslice.org,https://admin.ranch.cleanslice.org,https://api.ranch.cleanslice.org"
            - name: DATABASE_URL
              valueFrom:
                secretKeyRef:
                  name: ranch-external-db
                  key: uri
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: ranch-secrets
                  key: JWT_SECRET
            - name: JWT_EXPIRES_IN
              value: "7d"
            - name: BRIDLE_API_KEY
              valueFrom:
                secretKeyRef:
                  name: ranch-secrets
                  key: BRIDLE_API_KEY
            - name: WORKFLOW_PROVIDER
              value: "argo"
            - name: ARGO_WORKFLOWS_URL
              value: "http://argo-workflows-server.argo.svc.cluster.local:2746"
            # Paddock CLI dynamic-imports <runtimeRoot>/src/runtime.ts and
            # writes scenarios + reports under <runtimeRoot>/.paddock.
            # Image ships runtime at /usr/src/app/runtime.
            - name: PADDOCK_RUNTIME_ROOT
              value: /usr/src/app/runtime
            # Rancher template seed sources — image ships rancher/ at
            # /usr/src/app/rancher.
            - name: RANCHER_TEMPLATE_DIR
              value: /usr/src/app/rancher/.agent
            - name: RANCHER_PADDOCK_DIR
              value: /usr/src/app/rancher/.paddock/scenarios
            # Chat insight cron-batch: LLM-summarize settled chats every N seconds
            # (unset / <= 0 disables it; on-demand POST /chats/:id/summarize still
            # works either way — see ChatInsightService).
            - name: CHAT_INSIGHT_INTERVAL_SEC
              value: "900"
            # Browser-pool — shared headless Chromium for runtime tools.
            # Internal URL is reached over the in-cluster Service; public URL
            # is the user-facing host for VNC live views during 2FA logins.
            - name: BROWSER_POOL_INTERNAL_URL
              value: "ws://browser-pool.platform.svc.cluster.local:3000"
            - name: BROWSER_POOL_PUBLIC_URL
              value: "https://browser.ranch.cleanslice.org"
            - name: BROWSER_POOL_TOKEN
              valueFrom:
                secretKeyRef:
                  name: ranch-secrets
                  key: BROWSER_POOL_TOKEN
          volumeMounts:
            - name: paddock-work
              mountPath: /usr/src/app/runtime/.paddock
          readinessProbe:
            httpGet:
              path: /health
              port: 3333
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3333
            initialDelaySeconds: 30
            periodSeconds: 30
          resources:
            requests:
              cpu: 200m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 768Mi
      volumes:
        - name: paddock-work
          emptyDir:
            sizeLimit: 1Gi
---
apiVersion: v1
kind: Service
metadata:
  name: ranch-api
  namespace: platform
spec:
  selector:
    app: ranch-api
  ports:
    - name: http
      port: 80
      targetPort: 3333
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ranch-api
  namespace: platform
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.ranch.cleanslice.org
      secretName: ranch-api-tls
  rules:
    - host: api.ranch.cleanslice.org
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ranch-api
                port:
                  number: 80
